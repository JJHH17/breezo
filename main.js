import { initBtn, hideInitModal } from "./querySelectors.js";

// File responsible for main app functionality, includes utilization of the Visual Crossing API.
initBtn().addEventListener("click", () => {
    
})
